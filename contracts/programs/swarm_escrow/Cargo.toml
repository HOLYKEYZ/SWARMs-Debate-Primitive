[package]
name = "swarm-escrow"
version = "0.1.0"
description = "SWARMs Debate Primitive Escrow Program"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "swarm_escrow"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.30.1"
